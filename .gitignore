# Confidential data
.env               # Environment variables (always ignore!)
*.db               # Database files (e.g., SQLite)
*.sqlite3          # Django's default database
db.sqlite3         # Common Django database name

# Local logs (if they contain sensitive data)
*.log